import React from 'react';
import { View } from 'react-native';

// Esta es una pantalla "fake" que nunca se muestra
// Solo existe para que el tab funcione, pero el contenido real 
// es manejado por el componente UtilitiesMenu
const FakeToolsScreen = () => {
  return <View />;
};

export default FakeToolsScreen;